import { IDictionaryPair , Dictionary } from "util/collections/dictionary"
import { inject, bindable, customElement, bindingMode } from "aurelia-framework";

@inject(Element)
@customElement("metro-ui-appbar")
/**
 * MetroUIAppbar
 */
export class MetroUIAppbar {
 
    
 
}